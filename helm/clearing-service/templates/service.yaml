apiVersion: v1
kind: Service
metadata:
  name: {{ include "clearing-service.fullname" . }}
  labels:
    {{- include "clearing-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "clearing-service.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
